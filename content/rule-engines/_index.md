+++
title = "Rule engines"
description = "What a rule engine is, how businesses use one, and where Winnow fits."
sort_by = "weight"
weight = 5
# This landing page was merged into the site Overview (content/_index.md). The
# section is kept (without its own rendered page) so the twelve use-case pages
# below still live under /rule-engines/… and stay grouped. The home template reads
# `extra.groups` + these pages via `get_section` to render the color-coded cards.
render = false

[extra]
grouped = true
groups = [
  { slug = "risk", label = "Risk & compliance" },
  { slug = "growth", label = "Growth & personalization" },
  { slug = "ops", label = "Operations & data" },
]
+++
